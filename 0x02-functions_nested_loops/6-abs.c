#include "main.h"
/**

    _abs - Computes the absolute value of an integer.
    @num: The integer for which the absolute value is to be computed.
    Return: The absolute value of the given integer.
    */
    int _abs(int num)
    {
    if (num < 0)
    return (-num);
    else
    return (num);
    }
